Changes to run this: 

- cd backend
- npm install
- npm run dev

on a new terminal 
- cd frontend 
- npm install
- change the frontend/.env.example to .env
- Change value of VITE_KEY to any radom string
- npm run dev

